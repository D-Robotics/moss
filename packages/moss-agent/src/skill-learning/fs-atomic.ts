/**
 * Atomic file write — re-exported from the canonical implementation in utils/.
 *
 * See {@link ../utils/atomic-write.ts} for the full implementation and
 * DESIGN INTENT documentation.
 *
 * @internal
 */
export { atomicWriteFile } from '../utils/atomic-write.js';
